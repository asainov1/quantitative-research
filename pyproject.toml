[project]
name = "qr"
version = "0.1.0"
description = "Quant research: Binance Futures L2 collector + microstructure features + backtest"
requires-python = ">=3.11"
dependencies = [
    "orjson>=3.11.9",
    "pyarrow>=24.0.0",
    "python-dotenv>=1.2.2",
    "websockets>=16.0",
]
